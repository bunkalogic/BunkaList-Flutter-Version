import 'package:flutter/material.dart';

class AllDetailsSimilarTab extends StatelessWidget {
  const AllDetailsSimilarTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Similar Tab'),
      )
    );
  }
}