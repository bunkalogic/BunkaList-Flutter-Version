import 'package:flutter/material.dart';

class AllDetailsSeasonTab extends StatelessWidget {
  const AllDetailsSeasonTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Season Tab'),
      )
    );
  }
}