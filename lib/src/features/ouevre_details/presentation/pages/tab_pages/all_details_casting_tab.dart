import 'package:flutter/material.dart';

class AllDetailsCastingTab extends StatelessWidget {
  const AllDetailsCastingTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Casting Tab'),
      )
    );
  }
}