import 'package:flutter/material.dart';

class AllDetailsOpenningTab extends StatelessWidget {
  const AllDetailsOpenningTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Openning Tab'),
      )
    );
  }
}